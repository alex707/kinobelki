var com_field = document.getElementById('earler_comments_field');
var divTag = document.createElement("div");
divTag.innerHTML = "<%= j render @project.comments.where('id > ?', params[:last_com]).order(created_at: :desc) %>";
com_field.insertBefore(divTag, com_field.childNodes[0]);
